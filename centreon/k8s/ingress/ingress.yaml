apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: apache-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
  namespace: default
  labels:
    app: centreon
spec:
  ingressClassName: nginx
  defaultBackend:
    service:
      name: apache
      port:
        number: 80
  rules:
  - host: k8s.int.centreon.com
    http:
      paths:
      - path: /centreon
        pathType: Prefix
        backend:
          service:
            name: apache
            port:
              number: 80
      # - path: /centreon/install/
      #   pathType: Prefix
      #   backend:
      #     service:
      #       name: maintenance
      #       port:
      #         number: 80